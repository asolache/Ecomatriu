// Función para registrar el nickname
function registrarNick() {
    var nickname = prompt("Por favor, ingresa tu nickname:");
    if (nickname != null) {
        // Aquí puedes agregar la lógica para conectar con el contrato inteligente y registrar el nickname en la blockchain
        // Por ejemplo:
        // contrato.registrarNickname(nickname);
        alert("¡Nickname registrado con éxito!");
    }
}
